version: '3.4'
services:
  web:
    build: .
    image: web-django:latest
    entrypoint: ["./bin/entrypoint.sh"]
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/app
    ports:
      - "8000:8000"
